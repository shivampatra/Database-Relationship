<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile Card</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
            background: #0d1117;
            min-height: 100vh;
            overflow-x: hidden;
            color: #ffffff;
        }

        .card-container {
            display: flex;
            min-height: 100vh;
            position: relative;
        }

        .image-section {
            flex: 1;
            position: relative;
            overflow: hidden;
        }

        .background-image {
            width: 100%;
            height: 100%;
            object-fit: cover;
            object-position: center center;
            transition: transform 0.8s ease;
            transform: scale(1.05);
        }

        .image-overlay {
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: linear-gradient(
                135deg,
                rgba(13, 17, 23, 0.1) 0%,
                rgba(102, 126, 234, 0.3) 30%,
                rgba(118, 75, 162, 0.4) 60%,
                rgba(13, 17, 23, 0.8) 100%
            );
        }

        .floating-elements {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            text-align: center;
            z-index: 2;
        }

        .profile-ring {
            width: 140px;
            height: 140px;
            border-radius: 50%;
            border: 4px solid rgba(255, 255, 255, 0.8);
            padding: 8px;
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(20px);
            margin: 0 auto 20px;
            animation: pulse 2s infinite;
        }

        .mini-avatar {
            width: 100%;
            height: 100%;
            border-radius: 50%;
            object-fit: cover;
            border: 2px solid rgba(255, 255, 255, 0.5);
        }

        .floating-username {
            color: white;
            font-size: 28px;
            font-weight: 700;
            text-shadow: 0 4px 20px rgba(0, 0, 0, 0.8);
            margin-bottom: 10px;
            animation: glow 3s ease-in-out infinite alternate;
        }

        @keyframes glow {
            from { text-shadow: 0 4px 20px rgba(0, 0, 0, 0.8), 0 0 30px rgba(255, 255, 255, 0.4); }
            to { text-shadow: 0 4px 20px rgba(0, 0, 0, 0.8), 0 0 40px rgba(255, 255, 255, 0.7); }
        }

        .floating-id {
            color: rgba(255, 255, 255, 0.8);
            font-size: 14px;
            font-weight: 500;
            text-transform: uppercase;
            letter-spacing: 2px;
            text-shadow: 0 2px 10px rgba(0, 0, 0, 0.5);
        }

        .content-section {
            flex: 1;
            background: linear-gradient(135deg, #0d1117 0%, #161b22 50%, #21262d 100%);
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 60px;
            position: relative;
        }

        .content-section::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: 
                radial-gradient(circle at 20% 50%, rgba(102, 126, 234, 0.15) 0%, transparent 50%),
                radial-gradient(circle at 80% 20%, rgba(118, 75, 162, 0.15) 0%, transparent 50%);
            pointer-events: none;
        }

        .post {
            background: rgba(33, 38, 45, 0.9);
            backdrop-filter: blur(20px);
            border-radius: 32px;
            padding: 50px;
            box-shadow: 
                0 30px 80px rgba(0, 0, 0, 0.3),
                0 0 0 1px rgba(102, 126, 234, 0.2),
                inset 0 1px 0 rgba(255, 255, 255, 0.1);
            max-width: 500px;
            width: 100%;
            position: relative;
            overflow: hidden;
            transition: all 0.5s cubic-bezier(0.165, 0.84, 0.44, 1);
        }

        .post::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 6px;
            background: linear-gradient(90deg, #667eea, #764ba2, #f093fb, #f5576c);
            background-size: 400% 400%;
            animation: gradientShift 3s ease infinite;
        }

        .post:hover {
            transform: translateY(-10px) scale(1.02);
            box-shadow: 
                0 40px 100px rgba(0, 0, 0, 0.4),
                0 0 0 1px rgba(102, 126, 234, 0.4),
                inset 0 1px 0 rgba(255, 255, 255, 0.2);
        }

        .info-item {
            margin-bottom: 30px;
            padding: 20px;
            background: rgba(13, 17, 23, 0.8);
            border-radius: 20px;
            border: 1px solid rgba(102, 126, 234, 0.2);
            transition: all 0.4s ease;
            position: relative;
            overflow: hidden;
        }

        .info-item::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
            transition: left 0.6s;
        }

        .info-item:hover::before {
            left: 100%;
        }

        .info-item:hover {
            transform: translateX(10px);
            background: rgba(13, 17, 23, 0.9);
            border-color: rgba(102, 126, 234, 0.4);
        }

        .info-label {
            font-size: 12px;
            color: #7d8590;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 1.5px;
            margin-bottom: 8px;
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .info-value {
            font-size: 18px;
            color: #f0f6fc;
            font-weight: 600;
        }

        .email-icon::before {
            content: "📧";
            font-size: 16px;
        }

        .birthday-icon::before {
            content: "🎂";
            font-size: 16px;
        }

        /* Animations */
        @keyframes pulse {
            0% { box-shadow: 0 0 0 0 rgba(255, 255, 255, 0.4); }
            70% { box-shadow: 0 0 0 20px rgba(255, 255, 255, 0); }
            100% { box-shadow: 0 0 0 0 rgba(255, 255, 255, 0); }
        }

        @keyframes titleGlow {
            from { 
                text-shadow: 0 4px 20px rgba(102, 126, 234, 0.3);
                filter: brightness(1);
            }
            to { 
                text-shadow: 0 4px 30px rgba(102, 126, 234, 0.6);
                filter: brightness(1.2);
            }
        }

        @keyframes gradientShift {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }

        @keyframes slideInRight {
            from {
                opacity: 0;
                transform: translateX(50px);
            }
            to {
                opacity: 1;
                transform: translateX(0);
            }
        }

        @keyframes slideInLeft {
            from {
                opacity: 0;
                transform: translateX(-50px);
            }
            to {
                opacity: 1;
                transform: translateX(0);
            }
        }

        .image-section {
            animation: slideInLeft 1s ease-out;
        }

        .content-section {
            animation: slideInRight 1s ease-out 0.3s both;
        }

        .info-item:nth-child(1) { animation: slideInRight 0.8s ease-out 0.6s both; }
        .info-item:nth-child(2) { animation: slideInRight 0.8s ease-out 0.8s both; }

        /* Responsive Design */
        @media (max-width: 768px) {
            .card-container {
                flex-direction: column;
            }

            .image-section {
                height: 50vh;
            }

            .content-section {
                padding: 30px 20px;
            }

            .post {
                padding: 30px 25px;
                margin: 20px 0;
            }

            .floating-username {
                font-size: 24px;
            }

            .profile-ring {
                width: 120px;
                height: 120px;
            }

            .info-value {
                font-size: 16px;
            }
        }

        @media (max-width: 480px) {
            .content-section {
                padding: 20px 15px;
            }

            .post {
                padding: 25px 20px;
            }

            .floating-username {
                font-size: 20px;
            }

            .profile-ring {
                width: 100px;
                height: 100px;
            }
        }
    </style>
</head>
<body>
    <div class="card-container">
        <div class="image-section">
            <img src="<%= user.imageAvatar %>" alt="Profile Background" class="background-image">
            <div class="image-overlay"></div>
            <div class="floating-elements">
                <div class="profile-ring">
                    <img src="<%= user.imageAvatar %>" alt="Avatar" class="mini-avatar">
                </div>
                <div class="floating-username">@<%= user.username %></div>
                <div class="floating-id">ID: <%= user.id %></div>
            </div>
        </div>
        
        <div class="content-section">
            <div class="post">
                <div style="text-align: center; margin-bottom: 40px;">
                    <h1 style="
                        font-size: 36px;
                        font-weight: 800;
                        background: linear-gradient(135deg, #667eea, #764ba2, #f093fb);
                        -webkit-background-clip: text;
                        -webkit-text-fill-color: transparent;
                        background-clip: text;
                        margin-bottom: 8px;
                        text-shadow: 0 4px 20px rgba(102, 126, 234, 0.3);
                        animation: titleGlow 3s ease-in-out infinite alternate;
                    ">Profile Dashboard</h1>
                    <p style="
                        color: #7d8590;
                        font-size: 16px;
                        font-weight: 500;
                        letter-spacing: 1px;
                    ">Personal Information</p>
                </div>
               
                 <div class="info-item">
                    <div class="info-label email-icon">Your username</div>
                    <div class="info-value">@<%= user.username %></div>
                </div>
               
                <div class="info-item">
                    <div class="info-label email-icon">Email Address</div>
                    <div class="info-value"><%= user.email %></div>
                </div>
                
                <div class="info-item">
                    <div class="info-label birthday-icon">Your Password</div>
                    <div class="info-value"><%= user.password %></div>
                </div>

                 <div class="info-item">
                    <div class="info-label email-icon">Date of Birth</div>
                    <div class="info-value"><%= user.DoB %></div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>